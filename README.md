# Лабораторная работа 7

### Вариант 7
Закодировать кириллические буквы по Цезарю с переменным ключом, равным номеру буквы в слове + 3.

``` gcc -o a.out main.c check_space.c read-utf8.c caesar-utf8.c read-utf16.c caesar-utf16.c -Wall```

```./a.out -h```

В репозитории оставлены файлы для проверки. Пример:

```./a.out --utf16 <a-utf16.txt```

В utf16 файле есть non-BMP символы